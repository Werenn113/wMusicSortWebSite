meta {
  name: Playlists
  type: http
  seq: 2
}

get {
  url: {{base_url}}/spotify/playlists
  body: none
  auth: inherit
}

vars:post-response {
  id: res.body[1].id
}

settings {
  encodeUrl: true
  timeout: 0
}
